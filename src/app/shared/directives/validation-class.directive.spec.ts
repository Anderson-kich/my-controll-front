import { ValidationClassDirective } from './validation-class.directive';

describe('ValidationClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationClassDirective();
    expect(directive).toBeTruthy();
  });
});
